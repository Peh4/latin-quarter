<h1>COLORIEZ LE QUARTIER LATIN</h1>

<canvas id="can" width="1100" height="1026"> </canvas>

<ul id="infos">
	<% @infos.each do |info| %>

	<li>
		<input id="<%= info.id %>" type="checkbox"/>
		<label for="<%= info.id %>" ><%= info.nom %></label>

	</li>
	
	<% end %>
</ul>

<p><a href="#" id="clear" >effacer</a></p>

<%= form_for(@draw) do |f| %>

	<%= render 'shared/error_messages' %>

	<div class="field hidden">
	<%= f.label :data %><br />
	<%= f.text_area :data %>
  	</div>

  <div class="actions">
    <%= f.submit "valider" %>
  </div>
<% end %>

